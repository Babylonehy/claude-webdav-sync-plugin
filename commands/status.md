---
description: "Show WebDAV sync status"
---

Display the current sync status including connection state and last sync time.

## Usage

```bash
python ${CLAUDE_PLUGIN_ROOT}/scripts/webdav_sync.py status
```

## Output

Shows:
- Whether WebDAV is configured
- Connection status
- Last sync timestamp
- Last sync action (push/pull)